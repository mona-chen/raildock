# RailDock Frontend Environment
# Copy to .env and adjust as needed

# Backend API URL (uncomment to connect to real backend in development)
# VITE_API_BASE_URL=http://localhost:3001

# Force mock mode even when API_BASE_URL is set (development only)
# VITE_USE_MOCK=true
