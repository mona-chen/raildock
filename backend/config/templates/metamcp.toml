name = "Metamcp"
description = "MCP Aggregator, Orchestrator, Middleware, Gateway in one app"
category = "stack"
logo = "svgs/metamcp.png"

[[services]]
name = "app"
category = "app"
subtype = "docker"
docker_image = "ghcr.io/metatool-ai/metamcp:2.4"
source = { type = "docker"  }
  [services.scaling]
    web = 1
  [services.env]
    POSTGRES_HOST = "postgres"
    APP_URL = "https://${{ RAILDOCK_PUBLIC_DOMAIN }}"
    NEXT_PUBLIC_APP_URL = "https://${{ RAILDOCK_PUBLIC_DOMAIN }}"
    BETTER_AUTH_SECRET = "${{ secret() }}"
    TRANSFORM_LOCALHOST_TO_DOCKER_INTERNAL = "true"
  [services.checks]
    enabled = true
    wait = 10
    timeout = 5

[[services]]
name = "postgres"
category = "database"
subtype = "postgres"
  [services.env]
    POSTGRES_DB = "metamcp_db"
    POSTGRES_USER = "${{ secret() }}"
    POSTGRES_PASSWORD = "${{ secret() }}"
    [[services.storage]]
      host = "postgres_data"
      container = "/var/lib/postgresql/data"
  [services.checks]
    enabled = true
    wait = 10
    timeout = 5

[[links]]
from = "app"
to = "postgres"