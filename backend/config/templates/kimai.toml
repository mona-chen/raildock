name = "Kimai"
description = "Open source time-tracking app."
category = "stack"
logo = "svgs/kimai.svg"

[[services]]
name = "mysql"
category = "database"
subtype = "mysql"
start_command = "--default-storage-engine innodb"
source = { type = "docker"  }
  [services.env]
    MYSQL_DATABASE = "kimai"
    MYSQL_USER = "${{ secret() }}"
    MYSQL_PASSWORD = "${{ secret() }}"
    MYSQL_ROOT_PASSWORD = "${{ secret() }}"
    [[services.storage]]
      host = "kimai-mysql-data"
      container = "/var/lib/mysql"
  [services.checks]
    enabled = true
    wait = 5
    timeout = 20

[[services]]
name = "kimai"
category = "app"
subtype = "docker"
docker_image = "kimai/kimai2:apache"
source = { type = "docker"  }
  [services.scaling]
    web = 1
  [services.env]
    APP_SECRET = "${{ secret() }}"
    MAILER_FROM = "kimai@example.com"
    MAILER_URL = "null://null"
    ADMINMAIL = "admin@kimai.local"
    ADMINPASS = "${{ secret() }}"
    TRUSTED_HOSTS = "https://${{ RAILDOCK_PUBLIC_DOMAIN }}"
    [[services.storage]]
      host = "kimai-data"
      container = "/opt/kimai/var/data"
  [services.checks]
    enabled = true
    wait = 2
    timeout = 10

[[links]]
from = "kimai"
to = "mysql"