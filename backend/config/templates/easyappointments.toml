name = "Schedule Anything. Let's start with easy! Get the best free online appointment scheduler on your server, today."
description = "Schedule Anything. Let's start with easy! Get the best free online appointment scheduler on your server, today."
category = "stack"
logo = "svgs/easyappointments.png"
[[services]]
name = "easyappointments"
category = "app"
subtype = "docker"
docker_image = "alextselegidis/easyappointments:latest"
source = { type = "docker"  }
  [services.scaling]
    web = 1
  [services.env]
    SERVICE_URL_EASYAPPOINTMENTS_80 = ""
    BASE_URL = "${SERVICE_URL_EASYAPPOINTMENTS}"
    DB_HOST = "mysql"
    DB_NAME = "easyappointments"
    DB_USERNAME = "root"
    DB_PASSWORD = "${SERVICE_PASSWORD_EASYAPPOINTMENTS}"
  [services.checks]
    enabled = true
    wait = 2
    timeout = 10

[[services]]
name = "mysql"
category = "database"
subtype = "mysql"
docker_image = "mysql:8"
source = { type = "docker"  }
  [services.env]
    MYSQL_ROOT_PASSWORD = "${SERVICE_PASSWORD_EASYAPPOINTMENTS}"
    MYSQL_DATABASE = "easyappointments"
    [[services.storage]]
      host = "easyappointments-mysql-data"
      container = "/var/lib/mysql"
  [services.checks]
    enabled = true
    wait = 5
    timeout = 20

[[links]]
from = "easyappointments"
to = "mysql"
