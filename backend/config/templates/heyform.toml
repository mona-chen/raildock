name = "Heyform"
description = "Allows anyone to create engaging conversational forms for surveys, questionnaires, quizzes, and polls. No coding skills required."
category = "stack"
logo = "svgs/heyform.svg"

[[services]]
name = "heyform"
category = "app"
subtype = "docker"
docker_image = "heyform/community-edition:latest"
source = { type = "docker"  }
  [services.scaling]
    web = 1
  [services.env]
    APP_HOMEPAGE_URL = "https://example.com"
    SESSION_KEY = "BASE64_GENERATED"
    FORM_ENCRYPTION_KEY = "BASE64_GENERATED"
    MONGO_URI = "mongodb://mongo:27017/heyform"
    REDIS_HOST = "keydb"
    REDIS_PORT = "6379"
    REDIS_PASSWORD = "CHANGE_ME"
    [[services.storage]]
      host = "heyform-assets"
      container = "/app/static/upload"
  [services.checks]
    enabled = true
    wait = 5
    timeout = 5

[[services]]
name = "mongo"
category = "database"
subtype = "mongo"
docker_image = "percona/percona-server-mongodb:latest"
source = { type = "docker"  }
    [[services.storage]]
      host = "heyform-mongo-data"
      container = "/data/db"
  [services.checks]
    enabled = true
    wait = 5
    timeout = 5

[[services]]
name = "keydb"
category = "app"
subtype = "docker"
docker_image = "eqalpha/keydb:latest"
start_command = "keydb-server --appendonly yes"
source = { type = "docker"  }
  [services.scaling]
    web = 1
  [services.env]
    KEYDB_PASSWORD = "CHANGE_ME"
    [[services.storage]]
      host = "heyform-keydb-data"
      container = "/data"
  [services.checks]
    enabled = true
    wait = 5
    timeout = 5

[[links]]
from = "heyform"
to = "mongo"

[[links]]
from = "heyform"
to = "keydb"

[[links]]
from = "keydb"
to = "mongo"
