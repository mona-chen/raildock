name = "Joplin"
description = "Self-hosted sync server for Joplin"
category = "stack"
logo = "svgs/joplin.png"

[[services]]
name = "postgres"
category = "database"
subtype = "postgres"
docker_image = "postgres:16"
source = { type = "docker"  }
  [services.env]
    POSTGRES_PASSWORD = "CHANGE_ME"
    POSTGRES_USER = "user"
    POSTGRES_DB = "joplin"
    [[services.storage]]
      host = "joplin-postgresql-data"
      container = "/var/lib/postgresql/data"
  [services.checks]
    enabled = true
    wait = 10
    timeout = 5

[[services]]
name = "joplin"
category = "app"
subtype = "docker"
docker_image = "joplin/server:latest"
source = { type = "docker"  }
  [services.scaling]
    web = 1
  [services.env]
    APP_BASE_URL = "https://example.com"
    DB_CLIENT = "pg"
    POSTGRES_PASSWORD = "CHANGE_ME"
    POSTGRES_DATABASE = "joplin"
    POSTGRES_USER = "user"
    POSTGRES_PORT = "5432"
    POSTGRES_HOST = "postgres"
  [services.checks]
    enabled = true
    wait = 5
    timeout = 20

[[links]]
from = "joplin"
to = "postgres"
